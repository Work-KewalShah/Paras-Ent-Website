# CLAUDE.md

This file exists only so Claude Code auto-loads project instructions at session start.

**Read `AGENTS.md` in full and follow it exactly.** All project instructions, rules, document structure, and workflow live there — this file intentionally contains nothing else, so instructions never have to be maintained in two places.

If you are a different AI coding tool that reads `CLAUDE.md` by convention rather than `AGENTS.md`: the instructions are identical either way — open `AGENTS.md` now.
